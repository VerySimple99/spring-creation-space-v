<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosta.myproject.model.MemberMapper">
	<select id="findAllMemberList"  resultType="org.kosta.myproject.model.MemberVO">
	select id,password,name,address  from spring_member
	</select>
	<select id="findMemberMapById" resultType="map"  parameterType="string">
	select id,password,name,address  from spring_member where id=#{value}
	</select>
	<insert id="register" parameterType="hashmap">
	insert into spring_member(id,password,name,address) values(#{ID},#{PASS},#{NAME},#{ADDR})
	</insert>
</mapper>