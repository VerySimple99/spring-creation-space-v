spring.application.name=spring16-mybatis-join-resultmap-inst

logging.level.root=warn

logging.level.org.kosa.myproject=debug

# ==============================================
# 데이터베이스 연결 설정
# ==============================================
# MySQL 8.0 연결 정보
spring.datasource.url=jdbc:mysql://localhost:3306/kosa?serverTimezone=UTC
spring.datasource.username=joyofbeing
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================================
# MyBatis 설정
# ==============================================
# XML Mapper 파일 위치 지정
# mybatis.mapper-locations=classpath:mapper/*.xml
# 도메인 객체 패키지 별칭 설정 (XML에서 풀패키지명 대신 클래스명만 사용 가능)
 #mybatis.type-aliases-package=org.kosa.myproject.domain
# 카멜케이스 자동 매핑 활성화 (user_name -> userName)
mybatis.configuration.map-underscore-to-camel-case=true